# GitHub 发布与软件包规则

从 V0.1.0 开始，DeepTalk Studio 的每个正式版本号都必须在 GitHub 创建一个 Release（发布版本）。

## 什么会发布

- 每次完成一个正式版本：例如 `v0.2.0`、`v0.3.0`、`v1.0.0`。
- 必要的紧急修复：例如 `v0.2.1`。
- 范围较大的版本可以先创建 `v0.3.0-rc.1` 作为预发布，确认后再发布正式版。

每个 GitHub Release 必须包含：版本标签、普通人可读的更新说明、主要能力、已知限制、验证结果和升级注意事项。GitHub 会自动提供该版本的 ZIP 和 TAR 源码下载包。

## 什么不会单独发布

- 普通文档调整。
- 未完成的开发提交。
- 仅用于临时排查的改动。

这些改动仍写入 Git 历史和 CHANGELOG；只有形成清晰可用版本时才发布。

## 软件包规则

GitHub “软件包”用于真正可安装或可部署的成品，例如 Python 包、Docker 镜像或 Node 包。V0.1 目前是可在 Codex 中直接使用的工作流项目，发布一个空软件包没有实际价值，因此暂不启用。

当未来需要让用户通过安装命令使用 DeepTalk Studio 时，Codex 应在对应版本同时：

1. 生成经过测试的安装包。
2. 发布到 GitHub Packages 或合适的公开包平台。
3. 在 GitHub Release 中附上安装说明和文件校验信息。

## 每轮正式发布的固定流程

1. 完成代码、文档和 HANDOFF 更新。
2. 运行完整测试与必要的端到端检查。
3. 更新 CHANGELOG 的正式版本条目。
4. 创建不可随意移动的版本标签。
5. 创建 GitHub Release，并上传真正需要的附加文件。
6. 核验 Release 页面、标签和下载文件可用。
7. 在 HANDOFF 记录 Release 链接、验证结果和下一步给 ChatGPT 的话。

